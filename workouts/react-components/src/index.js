import React from 'react';
import ReactDOM from 'react-dom';

import App from './AppComponent';

// render the App component into <div id="react-app">
ReactDOM.render(<App />, document.getElementById('react-app'));